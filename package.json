{
  "name": "survivor-heatmap",
  "version": "1.0.0",
  "description": "Heatmap de alertas de sobrevivientes atrapados para gestión de emergencias",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build",
    "dev": "node server.js",
    "railway:build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.6.0",
    "node-cron": "^4.5.0"
  },
  "engines": {
    "node": ">=18"
  }
}
